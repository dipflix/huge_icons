name: huge_icons
description: "Dipflix`s export huge icons"
version: 0.0.1

environment:
  sdk: ^3.1.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  assets:
    - lib/fonts/huge_icons.ttf

  fonts:
    - family: huge_icons_dipflixs_export
      fonts:
        - asset: lib/fonts/huge_icons.ttf
